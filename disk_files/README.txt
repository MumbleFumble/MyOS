Place files here to include them in the MyFS disk image (build/disk.img).
These files will be accessible via `cat <filename>` or `exec <filename>` in the shell.

Example:
  echo "Hello from disk!" > disk_files/hello.txt
